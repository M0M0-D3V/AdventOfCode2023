var fs = require("fs");
// var file = fs.readFileSync("./D10/d10.txt", "utf-8");
var file = fs.readFileSync("./D10/test.txt", "utf-8");
// var file = fs.readFileSync("./D10/test2.txt", "utf-8");
var inputs = file.split("\r\n");

const partOne = (rows) => {
  let sum;
  return sum;
};

console.log(`Part One: `, partOne(inputs));
